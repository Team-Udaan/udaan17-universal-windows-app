<Page
    x:Class="udaan17_universal_windows_app.Architects"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:udaan17_universal_windows_app"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="Black"
    DataContext="{Binding DefaultViewModel, RelativeSource={RelativeSource Self}}">

    <Grid x:Name="LayoutRoot">
        <Grid.Background>
            <ImageBrush ImageSource="/Assets/architects.jpg" Stretch="Uniform" Opacity=".5"/>
        </Grid.Background>
        <Grid.ChildrenTransitions>
            <TransitionCollection>
                <EntranceThemeTransition/>
            </TransitionCollection>
        </Grid.ChildrenTransitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Title Panel -->
        <StackPanel Grid.Row="0" Margin="19,0,0,0">
            <TextBlock Text="udaan '17" Style="{ThemeResource TitleTextBlockStyle}" Margin="0,12,0,0"/>
            <TextBlock Text="&lt;Architects/&gt;" Margin="0,-6.5,0,26.5" Style="{ThemeResource HeaderTextBlockStyle}" CharacterSpacing="{ThemeResource PivotHeaderItemCharacterSpacing}"/>
        </StackPanel>
        <!--TODO: Content should be placed within the following grid-->
        <Grid Grid.Row="1" x:Name="ContentRoot" Margin="19,9.5,19,0">
            <ScrollViewer>

                <StackPanel>
                    <!--TextBlock Text="Team Udaan" Style="{ThemeResource FlyoutPickerTitleTextBlockStyle}" FontSize="20"/-->
                    <!--StackPanel Orientation="Horizontal">
                        <TextBlock Text="Version : " Style="{ThemeResource FlyoutPickerTitleTextBlockStyle}" VerticalAlignment="Center"/>
                        <TextBlock Text="  1.0.0.1" Style="{ThemeResource ComboBoxPlaceholderTextBlockStyle}"/>
                    </StackPanel-->
                    <ListView x:Name="listView" ItemsSource="{Binding Item}" Background="Transparent">
                        <ListView.ItemTemplate>
                            <DataTemplate >
                                <Grid Width="350">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="3*"/>
                                    </Grid.ColumnDefinitions>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="0"/>
                                        <RowDefinition Height="auto"/>
                                        <RowDefinition Height="auto"/>
                                        <RowDefinition Height="auto"/>
                                        <RowDefinition Height="*"/>
                                    </Grid.RowDefinitions>
                                    <Border Grid.Row="0" Grid.RowSpan="5" Grid.ColumnSpan="2" Background="Black" Opacity=".5"/>
                                    <Grid Grid.Column="0" Grid.RowSpan="5" Margin="5,5,0,5" Background="White">
                                        <Image Source="{Binding Category}"/>
                                    </Grid>
                                    <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding Name}" Style="{ThemeResource SubheaderTextBlockStyle}" Foreground="White" HorizontalAlignment="Right" Margin="0,5,15,2"/>
                                    <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding Title}" Style="{ThemeResource TitleTextBlockStyle}" Foreground="White" HorizontalAlignment="Right" Margin="0,0,15,2"/>
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Grid.Row="4" Grid.Column="1" Margin="0,0,5,5" VerticalAlignment="Bottom">
                                        <Border BorderBrush="White" BorderThickness="0" Margin="10,0,10,0">
                                            <Image Source="/Assets/Phone.png" Height="40" Width="40" Tapped="Phone_Tapped"/>
                                        </Border>
                                        <TextBlock Text="{Binding Contact}" Visibility="Collapsed"/>
                                        <Border BorderBrush="White" BorderThickness="0" Margin="10,0,10,0">
                                            <Image Source="/Assets/Mail.png" Height="40" Width="40" Tapped="Image_Tapped"/>
                                        </Border>
                                        <Border BorderBrush="White" BorderThickness="0" Margin="10,0,10,0">
                                            <HyperlinkButton NavigateUri="{Binding Git}">
                                                <HyperlinkButton.Content>
                                                    <Image Source="/Assets/Git.png" Height="40" Width="40"/>
                                                </HyperlinkButton.Content>
                                            </HyperlinkButton>
                                        </Border>
                                    </StackPanel>
                                </Grid>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </StackPanel>
            </ScrollViewer>
        </Grid>
    </Grid>
</Page>
